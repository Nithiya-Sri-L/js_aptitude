var stillSpeed=prompt("enter the stillSpeed");
var upSpeed=prompt("enter the upSpeed");
stillSpeed=parseInt(stillSpeed);
upSpeed=parseInt(upSpeed);
var streamSpeed=stillSpeed-upSpeed;
alert(streamSpeed);
